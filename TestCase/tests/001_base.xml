<?xml version="1.0" encoding="utf-8" ?>

<test-suite id="001" name="base">
  <test-case id="001">
    <script>
      
      // check whether debugger currently is supported
      if(debug == null) {
        alert('No debugger supported. Try start by test-case runner.');
        return;
      }

      // dummy call debug.assert, should not crash
      debug.assert(true);
      
      // debug.assert usage:
      //
      // assert( boolean_expression )   // false to raise assert runtime exception
      //
      // assert( result, expect )       // compare 'result' and 'expect', difference to 
      //                                // raise assert runtime exception
      
    </script>
  </test-case>

  <test-case id="002">
    <script>
      var t = debug.assert;
			
			var a = 4;
			var b = new Number(a);
			
      // toString to binary
			t( b.toString(2) == '100' );
			
    </script>
  </test-case>
</test-suite>